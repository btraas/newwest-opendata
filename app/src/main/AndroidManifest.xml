<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a00968178.comp3717.bcit.ca.opendata">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="NewWest Open Data"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.ListActivity.ActionBar">
        <activity android:name=".CategoriesActivity"
            android:theme="@style/AppTheme.ListActivity.ActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity   android:name=".DatasetsActivity"
                    android:parentActivityName="CategoriesActivity"
            ></activity>
    </application>

</manifest>